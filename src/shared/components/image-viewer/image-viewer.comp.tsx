import React from 'react';

export interface ISteperProps {
    
}

const Steper: React.FC<ISteperProps> = ({ }) => {
    return (
        <div>
            
        </div>
    );
}

export default Steper;